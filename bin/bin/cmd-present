#!/bin/bash
# Exit 0 only if EVERY named command is on PATH.
# Lets other scripts read declaratively: cmd-present satty || exit 0
for cmd in "$@"; do
  command -v "$cmd" >/dev/null 2>&1 || exit 1
done
